import { createGlobalStyle } from "styled-components"; // eslint-disable-line

const Global = createGlobalStyle`

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: 'Roboto', sans-serif;
        background-color: #222;
        color: #fff;
    }
`
export default Global;